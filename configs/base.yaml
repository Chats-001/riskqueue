project_name: RiskQueue
random_seed: 42
split:
  train_fraction: 0.70
  validation_fraction: 0.15
features:
  lookback_hours: [1, 6, 24]
  excluded:
    - oldbalanceOrg
    - newbalanceOrig
    - oldbalanceDest
    - newbalanceDest
    - isFlaggedFraud

